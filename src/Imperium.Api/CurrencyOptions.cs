namespace Imperium.Api
{
    // Configuration for currency handling
    public class CurrencyOptions
    {
        // Number of decimal places used by the currency (e.g. 2 for cents)
        public int DecimalPlaces { get; set; } = 2;
    }
}
